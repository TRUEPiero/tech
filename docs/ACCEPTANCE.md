# Проверка и приемка переноса Технобит

Дата: 07.09.2026. Основа - версия сайта 9. Этот документ фиксирует проверенное поведение прототипа и задает приемку реализации программиста.

## Выполнено при подготовке пакета

| Проверка | Результат |
| --- | --- |
| Сборка исходного приложения версии 9 | Успешна перед публикацией версии 9. В пакете не менялись компоненты приложения или логика калькулятора. |
| 12 существующих автоматических тестов | Все прошли 07.09.2026: шаблоны, доступ, сохранение и конфликт ревизий, управление, JSON, контакты и серверный вывод страниц. |
| Шаблоны опубликованного сайта | В calculator_settings нет сохраненных строк. Используются 30 стартовых шаблонов из исходников. |
| Примеры и документы | Примеры JSON созданы функциями приложения. Таблицы комнат сформированы из того же набора, что используется калькулятором. |
| Команда локальной миграции | Успешно создала calculator_settings в отдельной новой локальной базе через wrangler.local.jsonc. Рабочая база не менялась. |
| Ссылки и данные пакета | Относительные ссылки шести HTML-документов проверены; JSON шаблонов совпадает с исходниками. |

Полная визуальная приемка на всех десяти размерах и во всех указанных браузерах не завершена. Автоматические проверки серверного HTML и логики не подтверждают удобство интерфейса на физических телефонах.

## Сценарии калькулятора

| ID | Действие | Ожидаемый результат |
| --- | --- | --- |
| C01 | Добавить цокольный, первый, второй и третий этажи. | Один тип этажа не дублируется. Повторяющиеся комнаты допускаются. |
| C02 | Добавить две спальни, изменить свет только в одной. | Каждая имеет свой ID. Вторая спальня и ее шаблон не меняются. |
| C03 | Удалить комнату, затем этаж с комнатами. | Удаленные объекты исчезают из сводки и JSON; оставшиеся значения сохраняются. |
| C04 | Добавить кухню и изменить мокрые зоны с 2 до 3. | На вкладке «Датчики» сохраняется 3 мокрые зоны. Это еще не 3 датчика и не число кранов. |
| C05 | Увеличить общий счетчик релейных групп на 1 для вентилятора. | relayAndSocketsGroups возрастает на 1; отдельного счетчика вентиляторов нет. |
| C06 | Изменить наличие вентиляции и кондиционера. | Вентиляция меняется для всего объекта, кондиционер - только для выбранной комнаты. Производитель не запрашивается. |
| C07 | Добавить прихожую и коридор. | По стартовым шаблонам выбрана P4, выключатель «Нет». |
| C08 | Добавить спальню и кабинет. | Стартовая спальня: 4 группы света, 1 штора, выключатель на 4 клавиши. Кабинет: 3 группы света, 1 штора, выключатель «Нет». |
| C09 | Выбрать P4, P6, P8; поочередно выбрать выключатель на 4, 6, 8 клавиш и «Нет». | Панели независимы. Выключатель один; прежний тип заменяется. В итогах считаются устройства, а не клавиши. |
| C10 | Изменить свет, отопление, шторы и кондиционер, затем открыть «Управление». | Под комнатой показаны текущие количества. LED считается группами, без умножения на два. Номера «Помещение 1, 2» в этой подписи отсутствуют. |
| C11 | Изменить нагрузки в комнате с уже выбранным управлением. | Панель и выключатель не перезаписываются автоматически. Пользователь принимает решение по сводке. |
| C12 | Уменьшать количество до нуля. | Минус не дает отрицательных значений. |
| C13 | Указать пустую, нулевую или отрицательную площадь и подготовить запрос. | Запрос не готовится. После ввода положительной площади и добавления комнаты проверка проходит. |
| C14 | Скачать JSON до подготовки запроса. | contact = null; status = not_requested; delivery.status = not_sent. |
| C15 | Подготовить запрос для каждого из трех каналов. | Нужны один корректный контакт и согласие. В JSON только выбранный контакт; status = prepared_locally; отправки нет. |
| C16 | После просмотра JSON изменить комнату и снова открыть просмотр или скачать файл. | Новый снимок соответствует текущим значениям. Уже скачанный файл и ранее открытый снимок не изменяются задним числом. |
| C17 | Обновить страницу калькулятора. | Начинается новая конфигурация. Автосохранения клиентского проекта и импорта JSON в интерфейс пока нет. |
| C18 | Проверить нижний итог. | Количества обновляются сразу; цена имеет значение null до утверждения и подключения расчета. |

## Редактор типовых комнат

1. Посетитель может использовать калькулятор, но не сохранять шаблоны. Сервер отклоняет запись без роли владельца/администратора.
2. Владелец меняет шаблон, сохраняет и обновляет страницу. Новая комната получает сохраненные значения. Уже добавленные комнаты не перезаписываются.
3. Две вкладки открывают одну ревизию. После сохранения первой вторая получает HTTP 409. Повторная запись без обновления ревизии не затирает данные.
4. Отрицательное число, дробное число, число больше 100 в шаблоне, неизвестная комната, дублирование типа и неполный набор отклоняются.
5. Старую запись без controls можно прочитать с добавлением начальных значений управления в ответ. Чтение не меняет запись и ревизию в базе. Старый запрос записи без controls отклоняется.
6. При переносе на другой сервер заменить доверенную идентификацию Sites действующей авторизацией сайта. Не разрешать запись на основании заголовков, которые посетитель способен выставить сам.

## Главная страница

- Кнопки расчета открывают калькулятор. Запись на презентацию ведет к единственной нижней форме.
- Мобильное меню открывается и закрывается. FAQ раскрывает ответы.
- Лента содержит 10 видеокарточек. Открывается модальное окно; закрытие крестиком, кликом снаружи и Esc работает. Пока проверяется заглушка плеера.
- Фото шоурума и проектов статичны; у проектов нет «Подробнее».
- Два варианта блока ценностей сохранены для выбора. Решение, какой оставить в финальном сайте, еще не принято.
- Имя, телефон и согласие обязательны; email необязателен. Демонстрационный ответ формы не показывается как реально доставленная заявка.

## Адаптивность

| Viewport, CSS px | Основной сценарий |
| --- | --- |
| 320 × 568 | Компактный телефон |
| 375 × 667 | Телефон |
| 390 × 844 | Телефон |
| 412 × 915 | Крупный телефон |
| 768 × 1024 | Планшет |
| 1024 × 768 | Планшет / небольшой ноутбук |
| 1280 × 800 | Ноутбук |
| 1440 × 900 | Десктоп |
| 1920 × 1080 | Десктоп |
| 2560 × 1440 | Целевой максимум 2.5K |

Диапазоны перестроения: 320-479, 480-767, 768-1023, 1024-1439, 1440-2559 и от 2560 CSS px. Контейнер основного контента - до 1440 px. На более широком экране контент остается по центру.

На всех размерах проверить отсутствие горизонтальной прокрутки страницы, наложений, обрезанного текста и искаженных изображений. Модальные окна должны помещаться в экран, при необходимости иметь внутреннюю прокрутку. Минимальная интерактивная зона 44 × 44 px, целевая для Android 48 × 48 px. Учесть безопасные зоны iPhone, читаемость без масштабирования и доступность действий без наведения.

Изображения загружаются с srcset/sizes и WebP там, где подготовлены варианты. При замене изображений сохранить пропорции, ключевой объект при кадрировании и достаточное качество для Retina. Проверить нагрузку изображений на мобильной сети.

Браузеры: актуальные стабильные Safari на iPhone и macOS, Chrome на Android/macOS/Windows, Edge и Firefox. Зафиксировать фактически проверенные версии, размеры и найденные дефекты в отчете приемки.

## После реализации расчета стоимости

Проверить 250 м² и соседние значения; 6/7 релейных каналов; 3/4 LED-группы; 3/4 шторы; отсутствие мокрых зон; отсутствующие цены; фанкойлы; удаление последней комнаты; смену Lite/Pro. Контрольные примеры утвердить вместе с правилами подбора. Одни и те же артикулы, количества и сумма должны попадать в нижний итог и JSON.

Цена без полного правила или РРЦ помечается как неполная. Нельзя подменять неизвестную цену нулем или объявлять расчет щита завершенным на основании одной конфигурации.
